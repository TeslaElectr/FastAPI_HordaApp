from schemas import TypeCreateSchema
from schemas import TypeSchema


adgez_type = TypeCreateSchema(
    name="adgezia",
    description="For adgezitive for road production",
    )

corr_type = TypeCreateSchema(
    name="corrosia",
    description="for stop corrosia active",
    )

epoxy_type = TypeCreateSchema(
    name="epoxy_hardner",
    description="for epoxy",
    )

cotton_type = TypeCreateSchema(
    name="cotton",
    description="for cotton production",
    )